#include <stdio.h>

/*Escreva um algoritmo que solicite ao usuário um número inteiro a. Seu programa deve
inverter o sinal de a e exibir o resultado.*/

int main() {

    int a,c;

    printf("digite um número:\n");
    scanf("%i", &a);

    c=-(a);

    printf("%i\n",&a);

return 0;
}
